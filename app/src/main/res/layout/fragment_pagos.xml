<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:gravity="center_horizontal"
    android:background="?attr/colorSurface">

    <!-- 🔹 Encabezado centrado -->
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/tvTitulo"
        style="@style/TextAppearance.Material3.HeadlineSmall"
        android:text="Pagos del Contrato"
        android:textAlignment="center"
        android:textColor="?attr/colorPrimary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="6dp" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/tvSubtitulo"
        style="@style/TextAppearance.Material3.BodyMedium"
        android:text="Historial de pagos registrados"
        android:textAlignment="center"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp" />

    <!-- 🔹 Loading -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center">

        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/progressPagos"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:visibility="gone"
            app:indicatorSize="48dp"
            app:trackThickness="4dp"
            app:indicatorColor="?attr/colorPrimary" />
    </FrameLayout>

    <!-- 🔹 RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvPagos"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingBottom="8dp"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

    <!-- 🔹 Empty View -->
    <LinearLayout
        android:id="@+id/emptyView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        android:visibility="gone">

        <com.google.android.material.imageview.ShapeableImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/ic_money"
            android:contentDescription="@string/no_hay_pagos"
            app:strokeColor="?attr/colorOutline"
            app:strokeWidth="1dp"
            app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.Material3.Corner.Full"
            android:alpha="0.8" />

        <com.google.android.material.textview.MaterialTextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/no_hay_pagos"
            style="@style/TextAppearance.Material3.BodyLarge"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:layout_marginTop="12dp" />
    </LinearLayout>
</LinearLayout>
